---
---
<div class="rounded-lg border border-[var(--border-color)] p-5">
  <h2 class="text-xl font-semibold mb-4">首页签名（打字机）</h2>
  <div class="flex gap-3 mb-3">
    <button id="load-home-cover-btn" class="rounded border border-[var(--border-color)] px-4 py-2 hover:bg-[var(--hover-bg-color)]">加载</button>
    <button id="save-home-cover-btn" class="rounded border border-[var(--border-color)] px-4 py-2 hover:bg-[var(--hover-bg-color)]">保存</button>
  </div>
  <textarea id="home-cover-signatures" rows="12" class="w-full rounded border border-[var(--border-color)] bg-transparent px-3 py-2" placeholder="每行一句签名，例如：&#10;探索AI技术的无限可能 🤖"></textarea>
  <p class="text-xs mt-2 text-[var(--text-color)]/60">每行一句，打字机会循环播放。</p>
  <p id="home-cover-msg" class="text-sm mt-2 text-[var(--text-color)]/70"></p>
</div>
